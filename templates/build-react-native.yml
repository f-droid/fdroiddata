# PLEASE REMOVE ALL COMMENTS BEFORE SUBMITTING TO F-DROID DATA!

# Starting from the following template and grep for "npm" in the metadata
# directory to see other examples.
# See also https://f-droid.org/2020/10/14/adding-react-native-app-to-f-droid.html

Builds:
  - versionName: '1.0'
    versionCode: 1
    commit: 0000000000000000000000000000000000000000
    subdir: android/app
    sudo:
      - apt-get update
      - apt-get install npm
      # If you use yarn
      - npm -g install yarn
    init:
      - cd ../..
      # For NPM
      - npm install
      # For Yarn
      - yarn
    gradle:
      - yes
    srclibs:
      - firebase-stub@ce90a956aacda17a85c60577ee443aeb83d876ef
    prebuild:
      - cd ../..
      # Build expo libs from source
      - sed -i -e '1a "expo":{"autolinking":{"android":{"buildFromSource":[".*"]}}},'
        package.json
      # Build with JDK 21
      - sed -i '/jvmToolchain\|JavaVersion/s/17/21/' node_modules/@react-native/gradle-plugin/*/build.gradle.kts
        node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/utils/JdkConfiguratorUtils.kt
      # Some patches for different packages. Add when needed.
      - sed -i -e '/installReferrerVersion/,+12d' node_modules/react-native-device-info/android/build.gradle
      - sed -i -e '/installreferrer/d' node_modules/expo-application/android/build.gradle
      - sed -i -e '/com.android.installreferrer.api/d' -e '/StringBuilder()/,/^      })/d'
        -e '/getInstallReferrerAsync/apromise.resolve("")' 
        node_modules/expo-application/android/src/main/java/expo/modules/application/ApplicationModule.kt
      - sed -i -e '/firebase/d' node_modules/expo-notifications/android/build.gradle
      - cp -a $$firebase-stub$$/firebase-messaging/src node_modules/expo-notifications/android
      # Generate android files if it's not generated
      - npx expo prebuild -p android --clean
      - sed -i -e '/signingConfig /d' android/app/build.gradle
    scanignore:
      # Add other build.gradle files here is local maven repo for react-native is used
      # Binaries for react-native, JSC and Hermes can be ignored currently
      - node_modules/react-native/sdks/hermesc/linux64-bin/hermesc
      - node_modules/expo-modules-core/android/ExpoModulesCorePlugin.gradle
      - node_modules/expo-modules-autolinking/scripts/android/autolinking_implementation.gradle
    scandelete:
      - node_modules

AutoUpdateMode: Version
UpdateCheckMode: Tags
CurrentVersion: '1.0'
CurrentVersionCode: 1
